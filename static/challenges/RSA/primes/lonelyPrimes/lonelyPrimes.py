from Crypto.Util.number import getPrime, bytes_to_long

nbits = 1024

e = 0x10001

m = bytes_to_long(b"crypto{???????????????????????}")

n = getPrime(nbits)

c=pow(m, e, n)

print(f"n = {n}")
print(f"e = {e}")
print(f"c = {c}")